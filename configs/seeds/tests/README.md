# Synthetic Gate Test Seeds

Role: controlled synthetic claim cards for Tau Scaling gate isolation.

These seeds are local-runtime diagnostics only. They do not validate silicon, product performance, manufacturing capability, process-node equivalence, benchmark superiority, or a universal Tau Scaling law.

Primary runner:

```powershell
python scripts/benchmarks/run_synthetic_gate_suite.py
```
